import os
import csv

from api.models import *
from django.conf import settings

cvsFileName="nationalities2.csv"
BASE_DIR = settings.BASE_DIR
csvfile = os.path.join(BASE_DIR, 'csv\\{cvsFileName}'.format(cvsFileName=cvsFileName))

cr = csv.reader(open(csvfile,"r"),)
for row in cr: 
    print(row)